Install Docker:
sudo apt install docker.io -y
sudo usermod -aG docker $USER
sudo systemctl restart docker
sudo systemctl status docker 

Check Docker Version:
sudo docker --version

Check Process:
sudo docker ps 
sudo docker ps -a          #to check all process

